const Pill = ({ children }) => (
  <span className="inline-flex items-center rounded-full border border-gray-200 px-3 py-1 text-xs text-gray-600 bg-white shadow-sm">
    {children}
  </span>
);

export default Pill;
